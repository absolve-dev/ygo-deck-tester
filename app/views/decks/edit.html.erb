<h1>Editing Deck</h1>

<%= render 'form' %>

<script>
  var deck_array = [];
  var deck_string = '<%= raw @deck.contents %>';
  if(deck_string){
    deck_array = JSON.parse(deck_string);
  }
</script>
  
<%= link_to 'Show', @deck %> |
<%= link_to 'Back', decks_path %>
